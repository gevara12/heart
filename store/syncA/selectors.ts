
export const getActiveStep = (state: { syncA:{ activeStep: number } }) => state.syncA.activeStep;
export const getParsedData = (state: { syncA:{ parsedData } }) => state.syncA.parsedData;
export const getParsedUrl = (state: { syncA:{ parsedUrl } }) => state.syncA.parsedUrl;
